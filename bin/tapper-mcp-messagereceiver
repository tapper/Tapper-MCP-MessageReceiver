#! perl

use Tapper::MCP::MessageReceiver;
use App::Daemon qw(daemonize);
use English '-no_match_vars';


$App::Daemon::as_user    = "root";
my $daemon = Tapper::MCP::MessageReceiver->new;
daemonize();

$daemon->run();
