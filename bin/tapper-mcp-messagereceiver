#!/usr/bin/env perl

use Tapper::MCP::MessageReceiver;


my $daemon = Tapper::MCP::MessageReceiver->new_with_options(pidfile=> $ENV{TAPPER_MSG_RECEIVER_PIDFILE} || '/tmp/tapper_msg_receiver_pidfile');

$daemon->run();

warn($daemon->status_message);
exit($daemon->exit_code);
